[project]
name = "cw-memory-compiler"
version = "0.1.0"
description = "Auto-compiling knowledge base from Claude Code conversations"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk>=0.1.29",
    "python-dotenv>=1.0.0",
    "tzdata>=2024.1",
]

[tool.ruff]
line-length = 100
